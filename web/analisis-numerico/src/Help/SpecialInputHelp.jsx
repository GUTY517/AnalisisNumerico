import React from 'react';

const SecantHelp = () => {
	return (
		<div className="">
            Sample Input
			<ul>
				<li>exp(5) = e^5</li>
				<li>sin(exp(3)*ln(x)) = sin(e^3*log(x))</li>
				<li>ln(x) = log(x)</li>
			</ul>
		</div>
	);
};

export default SecantHelp;