read x0, tol, iter
    y0 = f(x0)
    d0 = f '(x0)
    count = 0
    error = tol + 1

    while y0 != 0 && d0 != 0 && error > tol && count < iter do

        x1 = x0 - (y0/d0)
        y0 = f(x1)
        d0 = f '(x1)
        error = abs((x1 - x0)/x1)
        x0 = x1
        count = count + 1

    end while

        if y0 == 0 then
                print  x0 "Is a root"   
        else
                if error < tol then
                        print x0 es una aproximación a la raiz con tolerancia tol
                else
                        if d0 = 0 then
                                print "could be a multiple root"
                        else
                                print "The method fails finding a root, reason: Maximum iterations reached"

                        end if
                end if
        end if   