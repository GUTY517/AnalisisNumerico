Function cuadratic_spline
	read xs, ys
	n = size(xs)
	numEcuation = 1 

	for numEcuation = 1 until n-1 do

		empalmar(xs[numEcuation], ys[numEcuation]) 
		numEcuation = numEcuation + 1

	end_for

	spliceEnd(xs[0], ys[0]) 
	spliceEnd(xs[n-1], ys[n-1])
	
	for numEcuation = 1 until n-1 do

		spliceDerivative(xs[numEcuation], ys[numEcuation])
		numEcuation = numEcuation + 1 

	end_for

	return splines
end Function cuadratic_spline