Function Lagrange
	read xs, fx, x
	n = size(x)
	L = getCoefficients(xs, x)
	acum = 0

	for i = 0 until n do

		acum = acum + (L[i] * fx[i])
	end_for

	retornar acum

end Lagrange

Function getCoefficients(xs, x)
	n = size(xs)

	for i = 0 until n do
		
		prod = 1
		
		for j = 0 until n do
			
			if i != j then
				prod = prod * (x – xs[j]) / xs[i] - xs[j]
			end_if

		end_for
	
	L[i] = prod
	
	end_for
	
	return L

end Function getCoefficients